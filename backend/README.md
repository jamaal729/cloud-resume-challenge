### Back end - View Count API

A simple express server to be called from the frontend code to handle view counts and must be running for the page counts to be updated

```sh
cd backend
npm i
npm start
```

View count endpoint is http://127.0.0.1:5174/api/views