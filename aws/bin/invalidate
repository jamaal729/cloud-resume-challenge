#!/usr/bin/env bash
set -e

# ansible-playbook playbooks/invalidate.yml --ask-vault-pass

ansible-playbook playbooks/invalidate.yml --ask-vault-pass -e "debug_mode=true" 2>&1 | grep -A 20 "stack_outputs"

# ansible-playbook playbooks/invalidate.yml -e "debug_mode=true" 2>&1 | grep -A 20 "stack_outputs"