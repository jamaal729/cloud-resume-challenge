gcp-key.json
.dist/

# --- Terraform ---
# Local .terraform directories (contains provider binaries and modules)
.terraform/
**/.terraform/*

# Terraform state files and backups (sensitive data; use a remote backend like GCP Cloud Storage instead)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (likely to contain sensitive data like passwords/keys). 
# Use a secrets manager or environment variables instead.
*.tfvars
*.tfvars.json

# Ignore override files used for local testing
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore plan output files (if generated with `terraform plan -out=tfplan`)
*.tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# .terraform.lock.hcl is typically committed to source control for consistent dependency resolution across teams, 
# but uncomment the line below if you wish to ignore it.
# .terraform.lock.hcl

# --- Ansible ---
# Ignore locally generated inventory files (e.g., dynamic inventories used for testing)
# Use a committed template file for shared inventory structures
/inventories/*_generated
/hosts/*_generated

# Log files generated during playbook runs
*.log
ansible.log

# Temporary or cache files generated by Ansible or Python
.ansible/
.ansible_module_cache/
.pytest_cache/
__pycache__/
*.pyc

# Optional: Ignore environment-specific configuration files not intended for version control
# e.g., if you have a local configuration file for testing:
# local_settings.py

# --- Operating System & IDE Files ---
.DS_Store
.idea/ # Ignore IDE-specific project files (e.g., PyCharm, IntelliJ)
*.swp # vim temporary files
