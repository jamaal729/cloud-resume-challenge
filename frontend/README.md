### Front End - React App
Developed from the static using React and Vite

```sh
cd frontend
npm i
npm run dev
```

App address is http://localhost:5173

Note: [backend](../backend) code has to be running to display the page visit count while running the frontend
